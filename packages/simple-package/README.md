# simple-package

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build simple-package` to build the library.

## Running unit tests

Run `nx test simple-package` to execute the unit tests via [Vitest](https://vitest.dev/).
